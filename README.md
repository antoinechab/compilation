
# regex to deterministic finite automata
```bash
cd regex_to_dfa
chmod +x index.py
./index.py
```
# lex exercice 57
```bash
cd lex
chmod +x compil.sh
<lexFileNumber>

./lex<lexFileNumber> < text.txt
```